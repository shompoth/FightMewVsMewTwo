import React from "react";

const Separator = () => (
    <div
        style={{
            height: "2px",
            backgroundColor: "lightgray",
            marginBlock: "15px",
            opacity: 0.6,
        }}
    ></div>
);

export default Separator;
